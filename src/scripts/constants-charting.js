const netProfitChart = null; // Initialize with null or the actual chart instance
const netCashingChart = null; // Initialize with null or the actual chart instance
const cashingPieChart = null; // Initialize with null or the actual chart instance
const hedgeBarChartA = null; // Initialize with null or the actual chart instance
const hedgeBarChartB = null; // Initialize with null or the actual chart instance
const hedgeBarChartC = null; // Initialize with null or the actual chart instance
const hedgeBarChartD = null; // Initialize with null or the actual chart instance
const dividentsChart = null; // Initialize with null or the actual chart instance
const revenuePieChart = null; // Initialize with null or the actual chart instance
const claimsPieChart = null; // Initialize with null or the actual chart instance
const stakesBarChart = null; // Initialize with null or the actual chart instance
const tokenomicsPieChart = null; // Initialize with null or the actual chart instance

const chartConfig = {
    // Set initial values in HTML, zeros on all widgets
    // Define CHART variables globally
    netProfitChart,
    netCashingChart,
    cashingPieChart,
    hedgeBarChartA,
    hedgeBarChartB,
    hedgeBarChartC,
    hedgeBarChartD,
    dividentsChart,
    revenuePieChart,
    claimsPieChart,
    stakesBarChart,
    tokenomicsPieChart,
}

export { chartConfig };
